#include <iostream>
using namespace std;
// in C++ all declarations and definitions must end with ;

int x; // define the global variable x

// a type of object called Fraction
class Fraction {

};

// you must declare before you can use
int main() {
  Fraction a; // create an object a of type Fraction sizeof(Fraction)= 1
  Fraction b; // create an object a of type Fraction sizeof(Fraction)= 1
	// object identity a and b must have different identity
	// in C++ location in memory is identity
	
	int y; // create an object y of type int

}



